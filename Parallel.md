# 分布式训练并行（Parallelism）完全指南

> **从单个 P 的原理到所有 P 的混合切分的完整梳理。**

大模型训练的核心矛盾是：**模型太大（单卡显存放不下）+ 算力不够（单卡算不完）**，解决手段就是"并行"——把模型、数据、计算按不同维度切分到多张卡上协同工作。但**切分只是第一步，切开的计算必须通过"通信"拼回正确结果**——所以本指南先从通信原语讲起（AllReduce / AllGather / ReduceScatter / All-to-All 是什么、怎么实现的），再逐个讲清每种并行（DP / TP / PP / SP / EP / FSDP）各自的"切什么、通信什么、解决什么问题"，最后把所有 P 串起来讲混合并行（3D / 4D 并行）如何在一个真实训练集群中协同切分。

---

# 〇、通信原语基础（一切并行的地基）

在讲各种并行之前，必须先讲清楚"卡与卡之间是怎么传数据的"。并行计算里的通信分为两类：**点对点通信（P2P）**——一张卡给另一张卡发数据（send/recv）；**集合通信（Collective）**——一组卡（一个通信组）共同参与、每个人都要收发、最终得到某种集体结果（AllReduce / AllGather / ReduceScatter / All-to-All / Broadcast 都属于集合通信）。**所有并行方式的通信开销，本质上都是这些原语的开销**：DP 用 AllReduce 同步梯度、TP 用 AllGather/AllReduce 拼层内结果、FSDP 用 AllGather + ReduceScatter 重构参数、EP 用 All-to-All 分发 token。所以先把这些原语的原理和成本模型吃透，后面讲每种并行时就能直接复用"通信量"的分析方法。

集合通信的实现由通信库完成——NVIDIA 卡上是 **NCCL**（NVIDIA Collective Communications Library），华为昇腾卡上是 **HCCL**；它们内部针对不同硬件拓扑实现了 Ring、Tree 等多种算法。下面逐个解释核心原语。

## AllReduce（全归约：大家的结果都一样）

### 1. 现有问题：为什么需要 AllReduce

AllReduce 是所有并行训练里**最常用、也最昂贵**的通信原语，它解决的核心问题是：**N 张卡各自算出了一份结果（比如梯度），如何让每张卡都拿到"所有卡结果的总和/平均值"？** 在数据并行（DP）中，每张卡用不同数据算出不同梯度，但更新参数必须用同一个全局梯度，否则 N 份模型就分叉了——这要求把 N 份梯度求和后广播给所有人，这正是 AllReduce 的语义：**输入是 N 份同形状的张量，输出是 N 份相同的张量，每个元素 = 所有输入对应元素之和（或均值、最大值等）。**

最朴素的做法是"主卡收集"：所有卡把结果发给卡 0，卡 0 求和后再广播回去。这有两大问题：第一，**卡 0 是瓶颈**——它要接收 N−1 份数据再发送 N−1 份，通信量是 O(N) 倍单卡数据量，卡 0 的带宽被灌满，其他卡空闲；第二，**单点故障**——卡 0 挂了整个通信就失败。所以需要一种"负载均衡、无中心"的 AllReduce 算法，让所有卡的通信量均等、带宽利用率最大化——这就是 Ring AllReduce。

### 2. 方法论：Ring AllReduce 和 Tree AllReduce 是怎么实现的

**Ring AllReduce（NCCL 默认，适合大数据量）**：把 N 张卡排成一个逻辑环（rank 0 → 1 → … → N−1 → 0），把要归约的张量平均切成 N 份（每卡持 1/N）。分两个阶段，逐步操作：

**阶段一（ReduceScatter 归约分散）**：做 N−1 轮。每轮每张卡把自己当前持有的第 k 份数据发给环上的下一张卡，同时从上一张卡收到一份数据并**逐元素相加**（归约）。N−1 轮后，每张卡恰好持有"所有 N 卡数据对应块之和"的**其中一份**（每卡一份完整归约结果的分片）。举例 N=4、数据切成 4 份：第 1 轮卡 0 发第 3 份给卡 1、卡 1 发第 0 份给卡 2……；3 轮后卡 0 持有"全部 4 卡第 0 份之和"，卡 1 持有"全部 4 卡第 1 份之和"，以此类推。

**阶段二（AllGather 全收集）**：再做 N−1 轮。每卡把自己手里那份"完整的归约分片"沿环继续传给下一张卡，同时接收上一张卡传来的分片。N−1 轮后，每张卡都收到了所有 N 份归约分片，拼起来就是完整的全局结果——**所有卡结果一致，AllReduce 完成**。

**Ring AllReduce 的成本模型**：每卡发送总量 = 2 × (N−1)/N × 数据量（两个阶段各 N−1 轮、每轮发 1/N 数据量）。当 N 很大时 ≈ 2 × 数据量，**与卡数基本无关**——这是它适合大数据量（如梯度同步）的根本原因。**Tree AllReduce（NCCL 备选，适合小数据量）**：构造一棵树（如二叉树），阶段一"reduce"从叶子向根逐层累加（每层只传一次全量数据），阶段二"broadcast"从根向叶子逐层广播。Tree 的每卡通信量 = 2 × log(N) × 数据量（每层全量），**通信量与 log N 成正比**——N 大时远高于 Ring，但**延迟（轮数）低**（2logN vs 2N），所以小数据量、延迟敏感的场合用 Tree 更划算。NCCL 会根据数据量大小自动在 Ring 和 Tree 之间选择。

### 3. 具体数值样例

假设 N=4 张卡，每卡有一个 8 元素向量（FP32，32 字节），需要 AllReduce 求和。**Ring 算法**：数据切成 4 份（每份 2 元素）。阶段一 3 轮、阶段二 3 轮，共 6 轮；每轮每卡发 2 元素（8 字节），每卡总发送 = 6 × 8 = 48 字节 = 1.5 × 单卡数据量（32 字节）。对照公式 2×(N−1)/N = 2×3/4 = 1.5 倍 ✓。**Tree 算法**（二叉树，深度 log₂4=2）：reduce 阶段每层每卡传全量 32 字节（层 1 两对节点各传一次、层 2 根收一次），broadcast 阶段对称，每卡总发送 ≈ 2 × 2 × 32 = 128 字节 = 4 × 单卡数据量。对比：N=4 时 Ring 通信量 48 字节 < Tree 128 字节，但 Tree 只需 4 轮 < Ring 6 轮。**随着 N 增大差距更明显**：N=64 时 Ring 每卡 ≈ 2× 数据量、Tree 每卡 ≈ 2×6=12× 数据量——所以**大模型梯度同步（几百 MB 到几 GB）必然选 Ring**。

> **面试一句话总结**：AllReduce 让 N 卡各自的结果求和后分发给所有人，Ring 算法把数据切成 N 份沿环两阶段（ReduceScatter + AllGather）流转，每卡通信量 ≈ 2× 数据量、与卡数无关，适合大梯度同步；Tree 算法延迟低但通信量随 logN 增长，适合小消息；NCCL 自动按数据量选择。

---

## AllGather 与 ReduceScatter（收集 / 归约的"半程"）

### 1. 现有问题：为什么需要这两个原语

AllReduce 可以拆成两个"半程"——先 ReduceScatter 再 AllGather。但这两个半程**单独拿出来也各自有用**，解决的问题是：**N 张卡各自持有完整数据的 1/N 分片时，如何"拼全"（AllGather）或"归约到分片"（ReduceScatter）？** 两个原语是互逆的关系：

- **AllGather（全收集）**：输入是 N 份**不同的分片**（每卡持有 1/N），输出是 N 份**相同的完整数据**（每卡拼出全量）。典型场景：TP 的列并行（每卡算出部分输出，AllGather 拼成完整输出）、FSDP 前向（每卡只有参数分片，AllGather 拼出完整参数再算）。
- **ReduceScatter（归约分散）**：输入是 N 份**完整的同形状张量**，输出是 N 份**不同的归约分片**（每卡持有全局结果的一部分）。典型场景：FSDP 反向（每卡有完整梯度，ReduceScatter 归约后每卡只留自己那份分片梯度）、Ring AllReduce 的阶段一。

**为什么需要它们**：如果每次都做完整 AllReduce（得到全量结果），通信量和显存都是全量级的；但很多时候我们**并不需要每卡都持有全量**——比如 FSDP 的参数本来就要分片存，那 AllGather 拼出来用完就丢、ReduceScatter 归约完只留分片即可，比"先 AllReduce 得到全量再切分"省一半通信量。理解这两个原语，是理解 FSDP（第 6 点）和 TP（第 2 点）的前提。

### 2. 方法论：AllGather / ReduceScatter 是怎么实现的

**AllGather** 的逐步操作（N 卡，每卡持有分片 X_i，目标是每卡得到 [X_0; X_1; …; X_{N−1}]）：与 Ring AllReduce 的阶段二完全相同——**第 1 步**，把环上相邻卡的分片沿环传递；**第 2 步**，重复 N−1 轮，每轮每卡把收到的分片存入本地结果的对应槽位、同时把自己的分片传给下一卡；**第 3 步**，N−1 轮后每卡收齐所有 N 份分片，拼接成完整数据。每卡发送量 = (N−1)/N × 数据量（发 N−1 次自己的分片）。也可以用树形实现：两两交换再合并（每轮合并规模翻倍，log N 轮），延迟更低。

**ReduceScatter** 的逐步操作（N 卡，每卡持有完整张量 A_i，目标：每卡 k 持有 Σ_i A_i 的第 k 份分片）：与 Ring AllReduce 的阶段一完全相同——**第 1 步**，把完整张量切 N 份；**第 2 步**，做 N−1 轮，每轮每卡把某份发给下一卡、从上一卡收一份并逐元素相加；**第 3 步**，N−1 轮后每卡 k 持有"所有 N 卡第 k 份之和"。每卡发送量同样 = (N−1)/N × 数据量。

**两者的关键特性**：通信量都是 (N−1)/N ≈ 1 倍数据量（与卡数无关），比 AllReduce 的 2 倍少一半——因为只需要"单向"传一次而不需要"归约+广播"两次。这也是 FSDP 相对 DP 通信不增反减的原因之一（见第 6 点）。

### 3. 具体数值样例

假设 N=4 卡、每卡持有 4 元素分片（FP32，16 字节）。**AllGather**：目标每卡拼出 16 元素的完整向量。做 3 轮：轮 1 每卡把自己的分片传给下一卡同时收到上一卡的分片；轮 2、轮 3 继续沿环传递，3 轮后每卡持有 4 份分片 = 16 元素 ✓。每卡发送 = 3 × 4 元素 × 4 字节 = 48 字节 = (N−1)/N × 完整数据量（64 字节）= 0.75 倍 ✓。**ReduceScatter**：每卡持 16 元素完整向量，3 轮归约后每卡持有 4 元素的"全局第 k 份之和"，每卡发送同样是 48 字节。**组合对比**：如果需求是"每卡最终只需要自己那份归约分片"（如 FSDP 梯度），用 ReduceScatter（48 字节）比先 AllReduce（96 字节）再丢弃 3/4 省一半通信——这就是 FSDP 用 AllGather + ReduceScatter 而不是 AllReduce 的原因。

> **面试一句话总结**：AllGather 是"分片拼全量"、ReduceScatter 是"全量归约到分片"，两者互逆、通信量都是 (N−1)/N ≈ 1 倍数据量，比 AllReduce（2 倍）省一半；它们是 Ring AllReduce 的两个半程，也是 TP（拼层内结果）和 FSDP（重构/归约参数梯度）的直接通信原语。

---

## All-to-All（全交换：每张卡给每张卡发不同数据）

### 1. 现有问题：为什么需要 All-to-All

AllReduce / AllGather / ReduceScatter 处理的是"每卡持有的数据是**同构**的"场景（都是同一份梯度、同一份参数分片，内容上是对称的）。但有一类场景**每卡要发给不同卡的数据完全不同**：**路由式通信**。最典型的是 MoE 模型的 Expert Parallelism（见第 5 点）：每个 token 被 router 分到不同的 expert（在不同卡上），卡 A 的 token 有一部分要去卡 B 的 expert 上计算——卡 A 发给卡 B 的是"token 1、2、3"，发给卡 C 的可能是"token 7、8"，**每对卡之间的数据内容、大小都不一样**。这种"所有人给所有人发各自不同的数据"就是 **All-to-All（全交换）**。

如果只用点对点通信实现（每张卡逐个 send/recv），会有两个问题：第一，N 张卡要组织 N−1 次发送 + N−1 次接收，**同步和调度极其繁琐**，容易死锁；第二，无法利用通信库对拓扑的优化。All-to-All 把这些封装成一次集体操作：**每个 rank 声明"我要给 rank j 发多少数据"，通信库统一调度，一轮完成所有交换**。

### 2. 方法论：All-to-All 是怎么实现的

All-to-All 的输入是"每卡持有 N 个缓冲（发给 rank 0..N−1 的），其中发给自己的那个保留不动"，输出是"每卡收到 N 个缓冲（来自 rank 0..N−1 的），按来源整理好"。逐步操作（N 卡）：

**第 1 步（准备发送缓冲）**：每卡把要发给各个 rank 的数据分别放到 N 个发送缓冲区（发给 rank 0 的放 buf[0]、发给 rank 1 的放 buf[1]……）。在 EP 场景中，这一步就是"按 token 的目标 expert 所在卡分组"。

**第 2 步（通信库调度）**：调用一次 All-to-All 原语，通信库根据拓扑选择最优路径（节点内走 NVLink、跨节点走 RDMA），执行所有 P2P 传输——每个 rank 把 buf[j] 发给 rank j，同时从 rank j 接收数据。这一步由 NCCL 的 `ncclAllToAll`（或其 P2P 组合实现）完成，通信模式上等价于 N×N 的矩阵转置（transpose）：发送矩阵的第 i 行 = rank i 发给所有人的数据，接收矩阵的第 j 列 = rank j 收到的数据。

**第 3 步（整理接收缓冲）**：每卡把收到的 N 份数据按来源排序，得到完整的"属于本卡"的数据（如本卡 expert 需要处理的所有 token）。

All-to-All 的关键特性：**通信量 = 每卡发出的总数据量 + 接收的总数据量**，且**各卡发送量可以不对称**（不像 AllReduce 要求对称）；它的开销主要由"被路由的数据量"决定，而不是模型大小——这也是 EP 通信量可控的原因。All-to-All 还有变体：**All-to-All-v（variable）**，允许每对卡之间的数据量可变（EP 场景必须用这种，因为 token 分布是动态的），实现上需要先做一次计数交换（告诉对方"我要给你多少数据"）再做数据交换。

### 3. 具体数值样例

假设 4 张卡做 EP（4 个 expert，每卡 1 个），每卡 batch 有 8 个 token，router 把 token 平均分给 4 个 expert（每个 expert 2 个 token，来自不同卡）。逐步演算卡 0 的 All-to-All：

```text
第 1 步：卡 0 的 8 个 token 被 router 分到 expert 0/1/2/3（卡 0/1/2/3）：
        → expert0(卡0): token A、B（留在本卡）
        → expert1(卡1): token C、D → 放入"发给卡 1"的缓冲
        → expert2(卡2): token E、F → 放入"发给卡 2"的缓冲
        → expert3(卡3): token G、H → 放入"发给卡 3"的缓冲

第 2 步：一次 All-to-All：卡 0 把 C、D 发给卡 1，收到卡 1 发来的
        属于 expert0 的 token；同时与卡 2、卡 3 各交换 2 个 token。

第 3 步：卡 0 收到来自卡 1/2/3 的各 2 个 token + 自己的 2 个 =
        共 8 个 token 全部属于 expert 0 → 在本地 expert 0 上计算。
```

通信量：每卡发送 6 个 token（发给其他 3 卡各 2 个）+ 接收 6 个，共 12 个 token 的传输量 = 1.5 倍"本卡 token 数"。如果 token 分布不均衡（比如 60% token 都去 expert 0），卡 0 会收到远超平均的 token——这就是 EP 负载均衡问题的通信侧体现（见第 5 点）。

> **面试一句话总结**：All-to-All 是"每张卡给每张卡发各自不同数据"的集体通信（等价于 N×N 矩阵转置），通信量由被路由的数据量决定且允许不对称，是 MoE 的 EP（token 按专家分发）的核心原语；动态数据量的场景用 All-to-All-v（先交换计数再交换数据）。

---

## 通信硬件与拓扑（NVLink / PCIe / InfiniBand / RDMA）

### 1. 现有问题：为什么通信原语的"速度"取决于硬件

集合通信算法（Ring/Tree）决定了"发多少数据、分几轮"，但**每轮实际能传多快，取决于底层硬件和拓扑**。同样的 AllReduce，在 NVLink 节点内和在 InfiniBand 跨节点之间，速度可以差一个数量级——这就是为什么并行方案要按"通信频率 × 硬件带宽"来设计（TP 必须放 NVLink 内、PP 可以跨节点，见第 7 点）。理解硬件层次是理解"为什么某种并行只能这样切"的物理基础。

常见的硬件层次与带宽量级（以 H100 时代为例）：**寄存器/SRAM**（片上，TB/s 级）→ **HBM 显存**（~3.35 TB/s）→ **NVLink**（GPU 间直连，单向 ~450 GB/s、双向 ~900 GB/s，用于同一节点内 GPU 互连，全互联拓扑）→ **PCIe**（CPU↔GPU 或经 CPU 转发，~64 GB/s，x16 Gen5）→ **InfiniBand / RoCE 网卡**（跨节点，400 Gbps = 50 GB/s 单向，走网络交换机）。**RDMA（Remote Direct Memory Access）**是跨节点通信的关键机制：允许网卡**绕过 CPU 直接读写远端 GPU/内存**（GPUDirect RDMA 甚至 GPU 直连网卡），省去 CPU 拷贝和内核中断，是跨节点低延迟高带宽的前提。**NVLink** 则是节点内 GPU 直连的私有互连，带宽是 PCIe 的近 7 倍，且拓扑上任意两卡都可直连（全互联），所以节点内通信几乎不经过 CPU/PCIe。

### 2. 方法论：通信库（NCCL/HCCL）是怎么利用硬件的

以 **NCCL** 为例，它的职责是"感知硬件拓扑 + 选最优算法 + 高效执行"。逐步操作：**第 1 步（拓扑探测）**：启动时通过 `ncclTopoDetect` 探测每张卡的 PCIe 位置、NVLink 连接、网卡归属，构建一张"哪些卡在一个 NVLink 域、哪些要跨节点"的拓扑图；**第 2 步（算法选择）**：根据数据量和拓扑选择算法——数据量大选 Ring（带宽最优），数据量小选 Tree（延迟最优），节点内/跨节点混用时用分层策略（节点内 NVLink 先归约、节点间再归约，即 hierarchical / 2-level 算法）；**第 3 步（建环/建树）**：按拓扑把卡排成最优通信图（Ring 的环要尽量让相邻卡物理相邻，减少跨节点跳数）；**第 4 步（执行）**：用 CUDA kernel 做数据切分、用 NVLink/PCIe/网卡驱动做传输、用 DMA 直接读写对方显存，全程不经过 CPU 参与数据搬运。华为昇腾的 **HCCL** 机制类似（面向昇腾卡与 HCCS 互连）。

**关键设计原则（面试常考）**：**通信越频繁的维度，越要放在带宽越高的层次**——TP 每层都通信（最频繁）必须放 NVLink 节点内；PP 段间通信稀疏可以跨节点走 IB；DP 每步一次梯度同步（几百 MB 到 GB 级）走 IB 但要选 Ring 算法压带宽。硬件带宽决定了"通信/计算比"，也就决定了哪种并行在哪种规模下划算。

### 3. 具体数值样例

对比同一个 AllReduce 在不同硬件上的耗时。假设要同步 14 GB 梯度（7B 模型 FP16 梯度的一半场景），三组硬件：

```text
① 单节点 8 卡 NVLink（聚合 ~900 GB/s 双向）：
   通信量 2×14 = 28 GB，耗时 ≈ 28/900 ≈ 31 ms
② 单节点内 PCIe（~64 GB/s，仅作对比）：
   耗时 ≈ 28/64 ≈ 437 ms —— 慢 14 倍，PCIe 不适合大通信
③ 跨节点 8 卡 InfiniBand 400 Gbps（50 GB/s 单向，双向聚合 100 GB/s）：
   耗时 ≈ 28/100 ≈ 280 ms —— 比 NVLink 慢 9 倍
```

**结论**：同样的梯度同步，NVLink 内 31 ms vs 跨节点 280 ms——差近 9 倍。这解释了：**为什么 TP 组必须限制在 NVLink 节点内**（TP 每层通信，放跨节点会慢到不可用）；**为什么 PP 适合跨节点**（段间通信稀疏，280 ms 摊到整个 micro-batch 流水线里可接受）；**为什么 DP 的梯度同步（每步一次）跨节点选 Ring 算法**（把 2× 通信量压到带宽极限）。硬件层次是选择并行维度的底层约束。

> **面试一句话总结**：通信速度由硬件层次决定——NVLink（节点内 GPU 直连，~900 GB/s）＞ InfiniBand/RDMA（跨节点，~100 GB/s）＞ PCIe（~64 GB/s）；NCCL/HCCL 负责探测拓扑、按数据量选 Ring/Tree/分层算法、用 DMA 高效执行；设计原则是"通信越频繁的维度越要放在高带宽层次"，这正是 TP 限 NVLink 内、PP 可跨节点的物理原因。

---

# 一、核心并行技术点

## 1. Data Parallelism（DP）

### 1. 现有问题：为什么要数据并行

Data Parallelism 是最早、最直观的并行方式，它解决的核心问题是：**单卡能装下模型、但训练太慢，需要多卡同时训练来提升吞吐。** 注意 DP 的前提是"模型能完整放进一张卡"——如果模型权重 + 优化器状态 + 激活显存超出单卡，DP 就无能为力了（那是 TP / PP / FSDP 的职责，见后文）。DP 的思路非常简单：**每张卡持有模型的完整副本，但各自吃不同的数据（不同的 mini-batch），每步各自算梯度，然后把梯度平均一下再同步更新参数。** 这样 N 张卡并行训练，理想情况下吞吐接近 N 倍。

DP 要解决的关键工程问题是**梯度同步的一致性**：如果每张卡各算各的、各更各的，那 N 张卡就变成了 N 个"独立但不同步"的模型，训练就失去了意义。必须保证每一步所有卡用的是**同一个模型状态**——这靠**每步结束后的梯度 AllReduce** 实现：把 N 份梯度（每卡一份，大小相同）求平均得到全局梯度，所有卡用同一个全局梯度更新参数，从而保证 N 份模型始终一致。DP 的代价也在这里：**每一步都要同步一次梯度，通信量正比于模型参数量**，与 batch 大小无关——模型越大，DP 的通信开销越大，这也是后面 ZeRO / FSDP 出现的原因之一。

### 2. 方法论：DP 是怎么实现的

DP 的逐步操作流程如下。假设有 N 张卡（rank 0 ~ N−1），每卡完整持有模型参数 θ：

**第 1 步（数据切分）**：把 global batch（大小为 G）平均切成 N 份 micro-batch，每卡拿到 G/N 条样本。例如 G=512、N=8，每卡 64 条。

**第 2 步（并行前向 + 反向）**：N 张卡**互不通信地**各自做前向和反向传播，每卡得到一份本地梯度 g_i（大小 = 参数量，例如 7B 模型 FP32 梯度约 28 GB）。

**第 3 步（梯度 AllReduce 求平均）**：执行 AllReduce 操作把 N 份梯度合并：$\bar{g} = \frac{1}{N}\sum_{i=0}^{N-1} g_i$。工程上常用 **Ring AllReduce**：把 N 张卡排成环，梯度切成 N 份，先做 N−1 次"传输 + 累加"（all-reduce 的 reduce-scatter 阶段）把每卡算出一份完整平均分片，再做 N−1 次"传输 + 复制"（all-gather 阶段）让每卡拿到完整平均梯度。总通信量 = 2 × (N−1)/N × 梯度大小（每卡收发各一次），随模型大小线性增长。

**第 4 步（同步更新）**：每卡用同一个全局平均梯度 $\bar{g}$ 更新自己的参数副本 θ ← θ − lr·$\bar{g}$。因为所有卡用的梯度完全相同，N 份模型始终保持一致。

DP 的关键特性是**通信量与 batch 无关、只与模型大小线性相关**：每步固定同步一次完整梯度。这意味着模型越大，DP 的"通信/计算比"越高，多卡收益越差——这正是 ZeRO 和 FSDP（见第 6 点）要解决的问题。

### 3. 具体数值样例

假设在 8 张卡（N=8）上训练一个 7B 模型，FP16 权重 14 GB、FP32 梯度 28 GB（7B × 4 字节）。global batch = 512，每卡 micro-batch = 64。

逐步演算每步的开销构成。**计算部分**：每卡每步处理 64 条样本，假设单卡每步前向 + 反向耗时 100 ms，8 卡并行后每步的计算时间是**仍是 100 ms**（8 卡同时算，总吞吐 = 单卡 8 倍）。**通信部分**：Ring AllReduce 需要同步 28 GB 的梯度，假设卡间带宽 100 GB/s（NVLink 聚合），一次 AllReduce 的通信量约 2 × 28 = 56 GB（reduce-scatter 28 GB + all-gather 28 GB），耗时约 560 ms——**通信时间远超计算时间**，这就是"通信/计算比"过高的典型表现：8 卡 DP 不仅没加速，反而比单卡还慢！

如果改用 **ZeRO/FSDP 分片**（见第 6 点）或减少梯度精度（如梯度通信也用 FP16/FP8），通信量可以显著下降；或者增大每卡的 micro-batch（batch 从 64 增到 256），让计算时间从 100 ms 涨到 400 ms，通信 560 ms 不变，通信/计算比从 5.6 降到 1.4，多卡收益才体现出来。这个例子说明：**DP 的适用条件是"通信可被计算掩盖"——要么模型别太大，要么 batch 足够大，要么用分片 + 低精度梯度压低通信量。**

> **面试一句话总结**：DP 让每卡持有完整模型副本、吃不同数据，每步通过 Ring AllReduce 同步梯度保证模型一致，实现吞吐随卡数近似线性扩展；它的瓶颈是每步通信量正比于模型大小，模型越大收益越差，需要靠 ZeRO/FSDP 分片或低精度梯度来缓解。

---

## 2. Tensor Parallelism（TP）

### 1. 现有问题：为什么要张量并行

Tensor Parallelism 解决的是 DP 解决不了的问题：**模型权重太大，单张卡根本放不下。** 一个 70B 参数的模型，FP16 权重就要 140 GB，远超单卡显存（H100 80 GB）；而且即使放得下，单卡也算了不动。DP 的前提是"每卡一份完整模型"，在模型超单卡显存时直接失效。TP 的思路是：**把"一层之内"的计算（一个 Transformer 层的权重矩阵）切开，分散到多张卡上，每卡只存一部分权重、算一部分矩阵乘法，通过通信把结果拼起来。** 这样模型整体被"摊"在多卡上，单卡显存需求降为 1/TP 大小。

TP 的难点在于**层内切分 + 通信的正确组织**：一个 Transformer 层里有多个线性层（QKV 投影、attention 输出投影、MLP 两个线性层），切开后哪些需要通信、通信多少，决定了 TP 的效率。TP 对卡间带宽极其敏感——它是**每层都要通信**的细粒度并行（不像 PP 只在层间传一次），所以 TP 通常只在 NVLink 高带宽的卡间（同一节点内）使用，跨节点做 TP 会因带宽不足而性能崩溃。

### 2. 方法论：TP 是怎么实现的

TP（以 Megatron-LM 的经典 1D 切分为例）的核心是**按矩阵乘法的"行"或"列"维度切分权重矩阵**，配合两种基本的通信原语。设权重矩阵 W 形状为 (out, in)，TP 大小 = T，每卡持有 W 的 1/T 分片。两种切分方式：

**列并行（Column Parallel）**：把 W 按输出维度竖切，W = [W₁; W₂; …; W_T]，每卡持有 (out/T, in) 的分片，算 Y_i = X·W_iᵀ（形状 (seq, out/T)），**最后把 T 份输出沿列拼接**（即把 Y_i 拼成完整的 (seq, out)）——拼接用 **AllGather** 通信。典型应用：QKV 投影、MLP 的第一个线性层。

**行并行（Row Parallel）**：把 W 按输入维度横切，W = [W₁ | W₂ | … | W_T]，每卡持有 (out, in/T) 的分片，每卡算 Y_i = X_i·W_iᵀ（X 也按列切成 X_i），得到部分和，**最后把 T 份部分和逐元素相加**——相加用 **AllReduce** 通信。典型应用：attention 输出投影、MLP 的第二个线性层。

**Transformer 层的 TP 完整流程**（逐步）：**第 1 步**，输入 X 复制到 T 卡（X 是完整的，各卡持有副本）；**第 2 步**，QKV 投影用列并行，每卡算自己的 Q_i/K_i/V_i（无需通信）；**第 3 步**，attention 在每卡内对各自的头计算（QKV 头也按卡切分），输出用行并行 AllReduce 合并；**第 4 步**，MLP 第一个线性层列并行、第二个行并行（第二次 AllReduce）；**第 5 步**，残差连接 + LayerNorm 在每卡完整计算（LayerNorm 需要完整激活，这里每卡都有完整副本，只是计算冗余——这就是 Sequence Parallelism 要消除的冗余，见第 4 点）。**每层总共 2 次 AllReduce**（attention 输出 + MLP 输出），通信量正比于每层的激活大小（seq × hidden），与序列长度和 batch 相关。

TP 的关键特性：**通信发生在每一层内部（细粒度），通信量正比于激活大小而非模型大小**——所以 TP 适合"大模型 + 小激活"（长序列会放大通信），并且必须跑在 NVLink 高带宽环境。TP 的显存收益是直接分割模型权重：70B 模型 TP=8 时每卡只需 17.5 GB 权重。

### 3. 具体数值样例

假设一个 70B 模型（hidden_dim = 8192，FP16），用 TP=8 部署在 8 张卡上，卡间 NVLink 带宽聚合约 450 GB/s（H100 8 卡）。逐步演算显存与通信。

**显存**：完整模型权重 140 GB，TP=8 后每卡持有 140/8 = 17.5 GB 权重（每层每个线性层只有 1/8 分片），加上 KV cache、激活，单卡显存需求大幅下降——这是 TP 能"装下"70B 模型的直接原因。

**每步通信量**：每层 2 次 AllReduce，每次 AllReduce 通信量 ≈ 2 × 每层激活大小。假设序列 2048 token、micro-batch 1，每层激活 = 2048 × 8192 × 2 字节 ≈ 32 MB（hidden state），attention 输出 + MLP 输出各一次 AllReduce，每层约 2 × 2 × 32 = 128 MB；70B 模型约 80 层，每步总通信量 ≈ 80 × 128 MB ≈ 10 GB。按 NVLink 450 GB/s 算，每步通信约 22 ms。对比每步计算时间：70B 每 token 前向反向约 2×70B×2×2048 ≈ 573 TFLOPs，8 卡 H100（每卡 989 TFLOPs）理论约 0.07 s = 70 ms。通信 22 ms / 计算 70 ms，占比约 24%——可以接受，但如果 TP=8 跨节点（PCIe/IB 带宽 100 GB/s 甚至更低），通信时间变成 100 ms，直接超过计算时间，TP 就得不偿失。这量化解释了为什么 **TP 必须限在 NVLink 节点内**。

> **面试一句话总结**：TP 把 Transformer 层内的权重矩阵按行列切分到多卡，列并行用 AllGather、行并行用 AllReduce，每层通信量正比于激活大小；它解决"模型超单卡显存"的问题，但通信细粒度、对卡间带宽要求极高，只能在 NVLink 高带宽环境使用。

---

## 3. Pipeline Parallelism（PP）

### 1. 现有问题：为什么要流水线并行

Pipeline Parallelism 解决的是**模型太大、且 TP 不够用**时的另一个切分维度：**按"层"切分模型**。当模型有几十上百层（如 70B 模型 80 层），可以按层切成若干段（stage），每张卡只负责其中一段的计算——卡 0 算第 1~20 层、卡 1 算第 21~40 层……数据像流水线一样依次流经各卡。PP 相比 TP 的核心优势是**通信量小得多**：TP 每层都要通信，PP 只在**段与段之间**传一次激活（forward 传激活、backward 传梯度），通信频率低、总量小，因此 **PP 可以跨节点（走普通高速网络）**，不像 TP 必须 NVLink。

PP 的核心难点是**流水线气泡（bubble）**：如果简单地"一个 batch 算完再传给下一段"，那任何时刻只有一段卡在算、其他卡全在等，利用率只有 1/P。解决思路是 **micro-batch 流水线**——把大 batch 切成很多 micro-batch，让它们像流水线一样在段之间流动，前一段算完第一个 micro-batch 就传给后一段、自己接着算第二个，从而让多张卡同时工作。但即使这样，**流水线启动和排空阶段仍然有气泡**，气泡大小取决于段数 P 和 micro-batch 数量 M，这是 PP 效率的核心指标。

### 2. 方法论：PP 是怎么实现的

PP 的逐步操作（以最经典的 GPipe 和优化的 1F1B 两种调度为例）。假设模型切成 P 段（stage 0 ~ P−1），每卡一个 stage；global batch 切成 M 个 micro-batch。

**GPipe 调度（朴素流水线）**：**第 1 阶段（前向）**，所有 M 个 micro-batch 依次流经 stage 0 → stage 1 → … → stage P−1（stage 0 算完 micro-batch 1 传给 stage 1 后立刻算 micro-batch 2，形成流水）；**第 2 阶段（反向）**，等所有 M 个前向完成并保存激活后，反向从 stage P−1 开始依次传回 stage 0。GPipe 的缺陷是**需要保存全部 M 个 micro-batch 的中间激活**（显存压力大），且前向结束到反向开始的切换存在一段"全部卡都在等"的时间。

**1F1B 调度（One-Forward-One-Backward，Pipedream 提出，现代主流）**：让每个 stage **交替执行前向和反向**，尽可能早地开始反向以释放激活显存：stage 先预热（往前向几个 micro-batch 直到流水线填满），然后每收到一个前向就**立即调度一个反向**（1 前向 1 反向交替），反向的梯度回传给前一段。1F1B 的好处是激活显存峰值从 O(M) 降到 O(P)（不需要保存所有 micro-batch 的激活），且气泡比 GPipe 更小。

**气泡的量化公式**：在理想 1F1B 下，流水线总共能满载计算的时间占比 = M/(M + P − 1)，**气泡比例 = (P−1)/(M + P−1)**。直观理解：流水线需要 P−1 个"拍"来填满（前向从第一段走到最后一段），又需要 P−1 个"拍"来排空（最后一段算完最后一个 micro-batch），气泡占比 = 启动+排空拍数 / 总拍数。**P 越大气泡越大、M 越大气泡越小**——所以 PP 的段数不能太多（通常 4~8），且 batch 要足够大。

PP 还有两个工程要点：**第一，显存不均衡**——每段只存自己那部分权重，但第一段和最后一段通常更吃紧（因为要存完整 embedding / LM head）；**第二，负载均衡**——各段的层数要尽量相等（按 FLOPs 均分而不是按层数均分，因为不同层的计算量不同）。此外现代框架常用 **交错流水线（interleaved schedule）** 把每个 stage 再拆成多个子块轮转调度，进一步压低气泡。

### 3. 具体数值样例

假设把 80 层模型切成 P=4 段（每段 20 层），global batch 切成 M=16 个 micro-batch，用 1F1B 调度。逐步演算气泡。

**气泡公式**：气泡比例 = (P−1) / (M + P−1) = 3 / (16 + 3) = 3/19 ≈ **15.8%**。也就是说 4 段流水线下，GPU 有效利用率约 84%，16 个 micro-batch 的流水线总耗时 = (M + P − 1) × 每拍时间 = 19 拍（单段算一个 micro-batch 的时间）。

**对比不同配置**：如果 P 不变、M 增大到 64：气泡 = 3/66 ≈ 4.5%，利用率 95.5%——**batch 越大气泡越小**；如果 M=16、P 增大到 8：气泡 = 7/22 ≈ 31.8%，利用率只有 68%——**段数越多气泡越大**，这就是为什么 PP 一般不超过 8 段。

**再看显存**：GPipe 需要保存 16 个 micro-batch 的中间激活（假设每 micro-batch 激活 8 GB，峰值 128 GB，直接爆显存）；1F1B 交替调度只需保存"流水线内还在反向的"约 P 个 micro-batch 的激活（约 4 × 8 = 32 GB），显存压力降为 1/4。**通信量**：PP 每 micro-batch 在段间只传一次激活（2048 × 8192 × 2 ≈ 32 MB）和一次梯度，相比 TP 每层 2 次 AllReduce，跨节点通信量小一个数量级——这就是 PP 能跨节点部署的原因。

> **面试一句话总结**：PP 按层把模型切成若干段、每卡只算一段，段间只传一次激活/梯度，通信量小、可跨节点；代价是流水线气泡（(P−1)/(M+P−1)），用 1F1B 交错调度 + 足够大的 micro-batch 数来压低气泡并减少激活显存。

---

## 4. Sequence Parallelism（SP）

### 1. 现有问题：为什么要序列并行

Sequence Parallelism 解决的核心问题是：**长序列下"激活显存"爆炸 + 部分算子无法被 TP 切分。** 前面讲过 TP 把权重矩阵切开，但注意 Transformer 里有两类算子的输入/输出不是"矩阵乘的中间结果"，而是**必须基于完整序列激活**的逐元素算子——LayerNorm、Dropout、残差连接。在 TP 下，每张卡都持有完整序列的激活副本（因为输入 X 是复制到各卡的），所以这些算子的激活在 T 张卡上**冗余存储了 T 份**：序列越长、batch 越大，这种冗余浪费越严重。对于一个长序列（如 32K token）的模型，**中间激活占用的显存可以超过模型权重本身**，成为训练/推理显存的主要瓶颈。

另一个动机来自**超长序列本身**：当序列长度超过单卡能处理的极限（注意力是 O(L²) 的显存/计算），需要把序列**按 token 维度切到多张卡**上并行处理——这就是"序列并行"的另一种含义：把一条长序列切成多个 chunk，分到多卡，各自计算部分注意力，再通过通信合并。两种含义（Megatron 的激活切分型 SP、以及长序列分块型 SP/CP）共享同一个思想：**沿着序列维度切分，减少单卡要"装"的序列数据**。

### 2. 方法论：SP 是怎么实现的

**Megatron-SP（激活切分型，最常用）**，与 TP 组合使用，逐步操作：**第 1 步**，在 TP 的基础上，把 LayerNorm / Dropout / 残差连接的**输入激活按序列维度切分**——每张卡只持有 (seq/T, hidden) 的序列片段，而不是完整 (seq, hidden)，从而让这些算子的激活显存降到 1/T；**第 2 步**，当需要进入 attention / MLP 的矩阵乘时，通过 **AllGather** 把序列片段恢复成完整序列（因为矩阵乘的输入 X 需要完整序列）；**第 3 步**，矩阵乘（列并行）算完输出激活后，再按序列维度切分（ReduceScatter），回到"每卡只存片段"的状态，继续下一层的 LayerNorm。如此每层只多出"一次 AllGather + 一次 ReduceScatter"的通信，但把 LayerNorm/Dropout/残差的激活显存从 T 份冗余降到 1 份。

**长序列分块型 SP / Context Parallelism（CP）**，处理超长序列（如 128K token）：把序列按 token 切成 C 块分到 C 张卡；每卡对自己的块做完整的 Transformer 前向，但 attention 部分需要**看到其他卡的 KV**——用 **Ring Attention / 分块 attention**：各卡持有自己的 Q 块，K/V 块在卡间以环状流动（每卡轮流接收其他卡的 KV block 并计算部分注意力分数），配合 online softmax（见 VLLM.md 第 10 点）维护 running max/sum，最后得到正确的全局注意力输出。每卡只需要 O(seq/C) 的 KV 显存，序列长度被 C 张卡均摊。

SP 的关键特性：**它切的是"序列"而不是"模型"或"数据"**——与 TP 叠加时消除激活冗余（Megatron-SP），与 DP 叠加时扩大 batch，单独/组合时让超长序列得以处理（分块型 SP/CP）。通信量：Megatron-SP 每层一次 AllGather + ReduceScatter（正比于激活大小，与 TP 的通信量同阶），分块型 SP 的 Ring Attention 通信量正比于序列长度。

### 3. 具体数值样例

假设 hidden_dim = 8192、序列长度 8192 token、micro-batch 1、TP=8，FP16。先算没有 SP 时的激活显存：每层 LayerNorm / Dropout / 残差的输入激活是完整 (8192, 8192) = 8192×8192×2 ≈ 134 MB，TP=8 时这 134 MB 在 8 张卡上**各存一份**，8 卡冗余共 8 × 134 ≈ 1 GB；模型 80 层，光这些算子的激活冗余就是 80 × 1 GB ≈ 80 GB 显存浪费（分布在各卡，每卡被占 10 GB 冗余）。

启用 Megatron-SP 后：LayerNorm 的激活按序列切分成 (8192/8, 8192) = (1024, 8192)，每卡只存 1024×8192×2 ≈ 16.8 MB，**每卡激活从 134 MB 降到 16.8 MB，8 卡总共只存 1 份**——80 层下来每卡节省约 80 × (134 − 16.8) MB ≈ 9.4 GB 显存，这对"显存吃紧的长序列训练"是决定性的。代价是每层多两次通信：AllGather 恢复 (8192, 8192) 激活（约 134 MB）+ ReduceScatter 切回，NVLink 下约 1 ms/层、80 层约 80 ms/步——相比省下的 80 GB 显存和因此能加大的 batch，通常非常值得。

再看长序列分块型：一个 128K token 的序列，单卡 attention 的 KV 显存 = 128K × 512 KB ≈ 64 GB（7B 级模型），单卡放不下；切成 C=8 块后每卡只处理 16K token 的 KV（约 8 GB），Ring Attention 让 8 卡协作算出完整注意力。这解释了为什么超长上下文（如 Mooncake / 长上下文推理）必然依赖序列维度的切分。

> **面试一句话总结**：SP 沿序列维度切分激活——Megatron-SP 把 LayerNorm/Dropout/残差的冗余激活降到 1/T 份（配合 TP 消除激活冗余），分块型 SP（Ring Attention / CP）把超长序列的 KV 摊到多卡；它切的是"序列数据"，是长序列训练/推理显存优化的关键维度。

---

## 5. Expert Parallelism（EP）

### 1. 现有问题：为什么要专家并行

Expert Parallelism 是 MoE（Mixture of Experts，混合专家）模型专属的并行方式，解决的核心问题是：**MoE 模型的 expert 数量多、参数量大，但每个 token 只激活少数 expert——用 DP 会每卡复制全部 expert（显存爆炸），用 TP 会让"只被少数 token 用到的 expert"占着多卡算力（浪费），且 DP 下不同 token 选中的 expert 分布不均导致严重的负载不均衡。** 回顾 MoE 结构：模型每层（FFN 部分）有 E 个 expert（每个是一个 FFN），router 把每个 token 路由到 top-k 个 expert 上。一个 8-expert、每 expert 1B 的 MoE 层，总参数 8B——如果 DP=8，每卡都要存全部 8 个 expert（8B 参数 × 8 卡冗余），显存浪费严重；而且 router 的 token 分配是动态的，DP 下各卡处理的 token 选中的 expert 完全不同，可能出现"卡 A 的 token 全挤在 expert 3，卡 B 的 token 全挤在 expert 7"的极端不均衡。

EP 的思路：**把 E 个 expert 按专家维度切分到多张卡上（每卡持有 E/T 个完整 expert），token 经 router 决定后，被"发送"到持有目标 expert 的卡上计算，算完再"送回"原卡。** 这样每个 expert 只存一份（不冗余），且 expert 的算力被所有卡的 token 共享（负载更均衡）。EP 是 MoE 大模型（如 DeepSeek、Mixtral 等）训练/推理的标配并行方式。

### 2. 方法论：EP 是怎么实现的

EP 的逐步操作（以 E 个 expert、EP 大小 = T 卡、每卡 E/T 个 expert 为例）：

**第 1 步（expert 分配）**：把 E 个 expert 均匀切分到 T 张卡，卡 k 持有 expert [k·E/T, (k+1)·E/T)。每卡持有**完整**的 router 参数（router 很小，各卡复制一份）。

**第 2 步（路由决策）**：每张卡对自己 batch 里的每个 token 调用 router，得到每个 token 选中的 top-k expert 编号。这一步在本地完成，无通信。

**第 3 步（Token 分发，All-to-All）**：这是 EP 的核心通信：每张卡按"token 要去哪个 expert（在哪个卡）"把 token 分组，然后执行 **All-to-All**（全交换）——每张卡把需要发往其他卡的 token 发送过去，同时接收其他卡发来的 token。通信量正比于被路由出去的 token 数。

**第 4 步（本地专家计算）**：每张卡对收到的 token，只在自己的 E/T 个 expert 上做 FFN 计算（每个 token 只走它选中的 expert，落在本卡的才在本卡算）。

**第 5 步（结果回收，All-to-All）**：计算完成后，再做一次 All-to-All 把结果送回 token 原来的卡，恢复原始 batch 顺序，继续后续层。

EP 的关键工程问题是**负载均衡**：如果 router 把 90% 的 token 路由到 expert 3（在卡 0），卡 0 会忙死、其他卡空闲。解决手段：一是**辅助负载均衡 loss**（在训练 loss 上加一项惩罚，鼓励 router 均匀分发）；二是**容量因子（capacity factor）**——给每卡分配略超均值的 token 容量，超出的 token 丢弃或走残差路径；三是**分组路由 / 专家分组**（把 token 分组，每组只能路由到一组 expert）。EP 常与 TP 组合：**每个 expert 内部用 TP 切分**（expert 太大时），即 EP 切专家、TP 切专家内部权重。

### 3. 具体数值样例

假设一个 MoE 层有 E=8 个 expert（每个 expert 是 2B 参数的 FFN），EP=4（4 张卡），每卡持有 2 个 expert。每张卡的 batch 有 128 个 token，router 把每个 token 路由到 top-2 expert。

**显存对比**：DP 方式（每卡存全部 8 个 expert）= 每卡 8 × 2B × 2 字节 = 32 GB；EP 方式每卡只存 2 个 expert = 4 GB，**显存降为 1/4**（EP 大小分之一），这是 EP 解决"expert 太多放不下"的直接收益。

**通信与负载**：每卡 128 个 token、每个 token 去 2 个 expert，共 256 次"token→expert"路由。理想均衡下每卡收到 4 卡 × 128 × 2 / 4 = 256 个"token-expert 对"（每个 token 的一半工作在本卡、一半在别的卡）。如果 router 不均衡（如 60% 的 token 选中 expert 0 和 expert 1，都在卡 0），卡 0 要处理 128×2×0.6 = 154 个 token 的工作量、其他卡只有约 102 个，**负载差约 1.5 倍**——最忙的卡决定步长时间，整体吞吐被拖累，这就是辅助均衡 loss 存在的意义。All-to-All 通信量：每卡发送约 128×2/4 × 2（发+收）× 激活大小，正比于 token 数和激活维度，通常远小于模型权重，可以接受。

> **面试一句话总结**：EP 把 MoE 的 expert 按专家维度切到多卡、每卡存 1/T 个完整 expert，通过两次 All-to-All（token 分发 + 结果回收）让所有卡的 token 共享全部 expert，解决 DP 下 expert 显存冗余和负载不均衡问题，是 MoE 大模型训练/推理的标配，常与 TP（专家内部）组合使用。

---

## 6. Fully Sharded Data Parallel（FSDP）

### 1. 现有问题：为什么要 FSDP

FSDP 解决的是 **DP 的显存浪费问题**：DP 每卡存完整模型 + 完整优化器状态，N 卡就是 N 份冗余。对于大模型，参数、梯度、优化器状态三块显存加起来非常可观——一个 7B 模型，FP16 参数 14 GB + FP32 优化器状态（Adam 的 m、v 各一份）约 7B×4×2 = 56 GB + FP32 梯度 28 GB，合计约 98 GB，单卡（如 80 GB）直接放不下，DP 直接失效。即使放得下，N 卡存 N 份也是巨大的浪费——明明可以每卡只存 1/N。

FSDP（Fully Sharded Data Parallel）的思路来自 **ZeRO-3**（DeepSpeed）：**把参数、梯度、优化器状态三者全部按卡分片**——每卡只存 1/N 的参数分片、1/N 的梯度分片、1/N 的优化器状态，训练时按需"临时拼出完整参数"用完即丢。这样显存占用随卡数线性下降，且**扩展了 DP 的语义**：DP 是"每卡完整副本"，FSDP 是"每卡 1/N 分片"，卡越多每卡越省显存。

### 2. 方法论：FSDP 是怎么实现的

FSDP 的逐步操作（N 卡，每卡持有 1/N 参数分片 θ_i）。FSDP 以**层为粒度**（每层一个 FSDP 单元）做分片与重构：

**第 1 步（前向：按层 AllGather 参数）**：处理第 l 层前，对该层的参数分片执行 **AllGather**，把 N 份分片拼成完整参数 θ_l（每卡临时持有完整层参数）；用完整参数做该层前向；**计算完立即丢弃**（释放完整参数，只保留自己的 1/N 分片），进入下一层时再做一次 AllGather。这样任意时刻显存里最多只有"当前层"的完整参数，而不是全部层的。

**第 2 步（反向：同样按层 AllGather）**：反向传播时同样按层 AllGather 恢复完整参数计算梯度；得到该层梯度 g_l 后，用 **ReduceScatter** 把梯度分片归约——每卡只保留自己的 1/N 梯度分片 g_i（其他卡的梯度分片通过通信汇总后丢弃），**梯度也是分片存储的**。

**第 3 步（更新：只更新自己的分片）**：每卡用自己的梯度分片 g_i 更新自己的参数分片 θ_i（优化器状态 m_i、v_i 也只存自己的分片）。因为每个参数分片只被一张卡更新，**无需任何跨卡同步**，更新是纯本地的。

**第 4 步（下一层重复）**：逐层执行上述流程，直到所有层处理完。

FSDP 的通信量分析：每层前向 1 次 AllGather + 反向 1 次 AllGather + 1 次 ReduceScatter，共约 3 次全量参数大小的通信（每次通信量 = 该层参数大小），**总通信量与 DP 的梯度 AllReduce 同阶**（都是"每步同步一次完整参数量"），但显存占用从 O(模型+梯度+优化器) 降到 O((模型+梯度+优化器)/N + 单层激活)。FSDP 还有两个常见变体：**ZeRO-1/2**（只分片优化器状态 / 优化器+梯度，参数不分片，通信更少、显存节省有限），以及 **CPU offload**（把参数分片进一步挪到 CPU 内存，GPU 显存几乎只放激活，代价是 PCIe 传输慢）。FSDP 也常与 TP 组合：**FSDP 做跨节点参数分片 + TP 做节点内层内切分**（见第 7 点混合并行）。

### 3. 具体数值样例

假设 7B 模型（FP16 参数 14 GB、FP32 梯度 28 GB、Adam 优化器状态 56 GB，合计 98 GB），N=8 卡，每卡 80 GB 显存。逐步演算三种方案的显存。

**DP（每卡完整副本）**：每卡需要 14 + 28 + 56 = 98 GB > 80 GB，**直接放不下**——DP 在 7B 模型上就失效了。

**ZeRO-2（只分片梯度 + 优化器）**：每卡参数 14 GB（完整）+ (28+56)/8 = 10.5 GB = 24.5 GB，放得下。

**FSDP（三者全分片）**：每卡 (14+28+56)/8 = 12.25 GB，加上激活（假设峰值 8 GB）共约 20 GB——**8 卡 FSDP 每卡只需 20 GB**，甚至可以在 40 GB 的卡上训练 7B。卡数越多省得越多：N=16 时每卡 6.1 GB + 激活 ≈ 14 GB。

**通信对比**：FSDP 每步通信量 ≈ 3 × 参数总量 = 3 × 14 GB = 42 GB（AllGather ×2 + ReduceScatter ×1）；DP 的梯度 AllReduce 每步 ≈ 2 × 梯度 = 2 × 28 = 56 GB。两者同阶（FSDP 甚至略少），但 FSDP 的显存收益是数量级的——**这就是"用几乎相同的通信量换 N 倍显存节省"**，代价是通信是"按层分片进行的"（每层一次 AllGather/ReduceScatter，小包多批，latency 略高）。这也解释了为什么大模型训练标配 FSDP/ZeRO 而非朴素 DP。

> **面试一句话总结**：FSDP 把参数、梯度、优化器状态全部按卡分片，前向/反向按层 AllGather 临时拼出完整参数、用完即弃，梯度 ReduceScatter 分片存储、更新只动自己的分片；它以与 DP 同阶的通信量换来了 O(N) 的显存节省，是超大模型训练（与 TP 组合）的标配。

---

# 二、并行架构：把所有 P 串起来（混合切分）

前面六种并行各有各的"切分维度"和"通信模式"，**真实的大模型训练从来不是只用一种，而是把它们组合成多维并行**。这一部分回答"这些 P 是怎么一起工作的"：先讲清楚每种并行适合切"什么维度"、通信发生在"什么层次"，再讲如何把它们叠成 3D / 4D 并行，最后给一个从零开始做混合切分的完整决策流程。

## 7. 3D 并行（DP + TP + PP）与多维混合并行

### 1. 现有问题：为什么需要把多个 P 组合起来

单一并行方式都有各自的"天花板"：**DP** 解决吞吐但要求模型放得下单卡（且通信量随模型线性增长）；**TP** 解决模型放不下但通信太细、只能在 NVLink 节点内；**PP** 解决模型放不下且可跨节点，但气泡随段数增长；**FSDP** 省显存但通信也是全模型量级。**没有任何一种并行能同时满足"模型超大 + 节点间组网 + 高吞吐"的全部需求**——70B、405B 甚至万亿参数的模型，动辄需要几百上千张卡，单靠任何一种并行都走不通。

举例：一个 405B 模型，FP16 权重 810 GB。如果只用 TP，TP=16 每卡也要 50 GB 权重 + 优化器状态，仍然超显存，而且 TP=16 的 AllReduce 通信量会大得无法接受；如果只用 PP，PP=32 的气泡率 = 31/(M+31) 高得离谱；如果只用 DP/FSDP，每卡要处理完整模型的前向（算力浪费且中间激活超大）。**只有把多个维度叠加：层间切（PP）× 层内切（TP/SP）× 数据复制（DP/FSDP）× 专家切（EP）**，才能把"模型规模、卡间拓扑、显存、吞吐"四个约束同时满足。

### 2. 方法论：3D 并行是怎么组合的

3D 并行（DP × PP × TP）是 Megatron-LM 确立的经典组合，组合方式遵循一个核心原则：**把通信频繁的维度放在"近"的卡之间，把通信稀疏的维度放在"远"的卡之间。** 具体来说：

- **TP（层内切分，通信最频繁）**：放在**同一节点内、NVLink 直连**的卡之间。TP 每层都要 AllReduce，必须最高带宽；
- **PP（层间切分，通信稀疏）**：放在**节点之间**（跨节点走高速网络，如 InfiniBand / RoCE）。PP 只在段间传激活/梯度，通信频率低，跨节点可接受；
- **DP / FSDP（数据复制，通信为梯度同步）**：放在**剩余的所有卡之间**（跨节点），每步一次梯度 AllReduce / ReduceScatter，通信量可控。

**逻辑组织**：先把 T 张卡组成一个 TP 组（TP=T，共享层内切分）；再把 P 个 TP 组串成流水线（PP=P，组间传激活）；最后把 D 个这样的"TP组×PP链"复制成数据并行副本（DP=D）。总卡数 = **T × P × D**。在 Megatron 的实际实现中，卡号按"先 TP、再 PP、最后 DP"的顺序排列（即 rank = d × (P×T) + p × T + t），保证 TP 卡在物理上相邻。

**4D / 多维扩展**：在这个 3D 骨架上叠加更多维度——**SP** 叠加在 TP 上（TP 组内做序列切分，消除激活冗余，见第 4 点）；**EP** 叠加在 MoE 模型的专家层上（专家维度切分 + All-to-All，见第 5 点）；**FSDP** 可以替代或补充 DP（把 DP 副本内部再做参数分片，进一步省显存）。现代超大模型训练（如 DeepSeek、GPT-4 级别）实际是 **TP + PP + DP + EP（+ SP + FSDP/ZeRO）的多维混合**，每一维都在自己的"适用层"上切分。

### 3. 具体数值样例

假设有 64 张卡（8 节点 × 8 卡，节点内 NVLink、节点间 InfiniBand），训练一个 70B 模型。逐步演算如何做 3D 切分。

**第 1 步（定 TP）**：TP 必须限在节点内，所以 T ≤ 8。取 **TP=4**（4 张卡一组做层内切分），70B 模型权重每卡 140/4 = 35 GB——为了留显存给优化器和激活，也可以取 TP=8（每卡 17.5 GB）。假设取 **TP=8**，一个节点正好是一个 TP 组。

**第 2 步（定 PP）**：层间切分跨节点，取 **PP=4**（4 个节点串成流水线，每节点 1 个 stage）。70B 约 80 层，每段 20 层。如果 micro-batch 数 M=32，气泡率 = 3/(32+3) ≈ 8.6%，可接受。

**第 3 步（定 DP）**：总卡数 64 = T(8) × P(4) × D，则 **D = 64 / (8×4) = 2**。即 2 个数据并行副本，每个副本是一个"8 卡 TP 组 × 4 段 PP 链"。

**第 4 步（汇总）**：最终配置为 **DP=2 × PP=4 × TP=8**。显存：每卡权重 17.5 GB + 优化器状态（70B×4×2 / 64 ≈ 8.75 GB）+ 激活（TP/SP 削减后），共约 35 GB，80 GB 卡绰绰有余。每步通信：TP 组内 AllReduce（NVLink，快）+ PP 段间激活（IB，稀疏）+ DP 间梯度 AllReduce（每步一次 140 GB 梯度 / 2 副本 ≈ 跨 32 卡）。

**再加 EP**：如果这是 MoE 模型（如 expert 层 64 个 expert），在 expert 层用 **EP=8**（8 卡各持 8 个 expert），与 TP 组重叠（专家内部再 TP）。于是最终是 **DP × PP × TP × EP 的 4D 并行**——每一维各管一段：TP 切层内权重、PP 切层、DP 切数据、EP 切专家。这就是真实超大模型训练集群的形态。

> **面试一句话总结**：3D 并行 = DP × PP × TP，按"通信频率决定物理距离"组合——TP 在 NVLink 节点内（层内、频繁通信）、PP 跨节点（层间、稀疏通信）、DP 在所有副本间（数据、每步梯度同步）；再叠加 SP（激活）、EP（专家）、FSDP（分片）就构成 4D/多维混合并行，总卡数 = 各维度乘积。

---

## 8. 混合切分的完整决策流程

### 1. 现有问题：面对一个具体训练任务，怎么选并行方案

前面讲了所有并行方式的原理，但面试和实际工作中真正的问题往往是：**"给我一个模型 + 一个集群，我该怎么切？"** 没有决策框架的话，会陷入"每种并行都能用、不知道用哪个"的困境。这一部分把前面的知识串成一个**从模型出发、逐步决策**的完整流程，回答"怎么从零开始做混合切分"。

决策的输入有三个：**模型规模**（参数量、层数、是否 MoE）、**集群拓扑**（单卡显存、节点内带宽 NVLink、节点间带宽 IB/RoCE、总卡数）、**训练约束**（global batch 大小、序列长度、目标吞吐）。决策的目标是：**在不爆显存的前提下，最大化吞吐（最小化通信/计算比）**。

### 2. 方法论：从模型到并行配置的逐步决策

**第 1 步（显存检查：单卡能否装下模型？）**：计算模型总显存需求 = 参数 + 梯度 + 优化器状态 + 峰值激活。若单卡能装下（如 7B 级），优先考虑纯 DP / FSDP（最简单、吞吐最高）；装不下则进入下一步。这决定了"是否需要切模型"。

**第 2 步（切模型：先 TP 还是先 PP？）**：模型装不下时，优先在**节点内用 TP** 切层内权重（TP 通信最频繁、必须 NVLink；且 TP 不引入气泡，比 PP 效率高）。TP 大小受两个约束：单卡显存（切到能装下为止）和节点内卡数（T ≤ 节点卡数）。如果 TP 切到节点上限还不够，再引入 **PP** 按层切（跨节点）。判断 PP 段数：P = 总卡数 / (T × D)，且 P 受气泡约束（通常 ≤ 8）。

**第 3 步（扩展吞吐：DP / FSDP）**：剩余卡数作为 **DP** 维度复制副本（D = 总卡数 / (T×P)）。如果模型已经很大、显存仍紧，用 **FSDP/ZeRO** 替代朴素 DP（分片省显存，通信量与 DP 同阶）。DP/FSDP 的 batch 约束：global batch = D × 每卡 micro-batch × 梯度累积，要匹配数据集的 batch 需求。

**第 4 步（特殊结构：MoE → EP，长序列 → SP）**：若模型是 MoE，在 expert 层加 **EP**（专家切分 + All-to-All，EP 大小通常 = 节点内卡数或按 expert 数均分）；若序列很长 / 激活显存吃紧，在 TP 组内加 **SP**（序列切分，消除激活冗余）。

**第 5 步（验证通信/计算比）**：估算每步通信量（TP 每层 AllReduce × 激活、PP 段间激活、DP 每步梯度、EP 两次 All-to-All）与每步计算时间，确保通信时间 ≤ 计算时间（否则考虑换维度：TP→PP、加梯度累积、用低精度梯度等）。

**一个经验法则（背下来面试用）**：**TP 解决"单卡放不下"，PP 解决"节点内放不下"，DP/FSDP 解决"不够快"，EP 解决"MoE 专家太多"，SP 解决"序列太长"**——每个 P 都有自己明确的"主责"，混合切分就是让每个 P 各司其职。

### 3. 具体数值样例

**场景**：8 节点 × 8 卡 = 64 卡，单卡 80 GB，节点内 NVLink、节点间 400 Gbps IB。要训练 70B dense 模型（FP16 权重 140 GB），global batch 目标 1024。逐步走决策流程：

```text
第 1 步：模型总需求 = 140(参数) + 280(FP32梯度) + 560(Adam状态) ≈ 980 GB
        >> 单卡 80 GB → 必须切模型。

第 2 步：先 TP：T=8（整节点 NVLink），每卡权重 17.5 GB。
        70B ≈ 80 层 → PP：每节点一个 stage，P=8 时气泡 = 7/(M+7)，
        取 M=64 → 气泡 ≈ 9.9% 可接受；但 8 段 × 8 卡 = 64 卡已用完，
        DP 就没有了 → 改为 P=4、D=2（见下）。

第 3 步：64 卡 = T(8) × P(4) × D(2)。DP=2：global batch 1024 → 每卡
        1024/(2×梯度累积)。设梯度累积 G=8，则每卡 micro-batch = 1024/2/8 = 64。
        显存：17.5(权重) + 980/64 ≈ 15.3(梯度+优化器分片) + 激活 ≈ 45 GB < 80 GB ✓

第 4 步：dense 模型无需 EP；序列 4K 不算超长，SP 可选（加了更省激活）。

第 5 步：通信/计算比——TP 组内 AllReduce（NVLink 快）、PP 段间激活
        （4K×8K×2B×2 ≈ 128 MB × 每步 2 次 × 跨 4 段，IB 400Gbps ≈ 5 ms）、
        DP 梯度 AllReduce（140 GB/2 副本跨 32 卡，IB 聚合 ~12.8 Tbps ≈ 90 ms）
        vs 计算 ~200 ms/步 → 通信占比 ~50%，可通过增大 micro-batch 或
        梯度压缩优化。最终配置：DP=2 × PP=4 × TP=8（+ 可选 SP）。
```

**对比另一场景**（MoE 模型 70B 含 64 experts、每 expert 1B）：第 4 步就要加 **EP=8**（每卡 8 个 expert，显存 8×1B×2 = 16 GB），于是配置变为 **DP × PP × TP × EP** 的 4D 并行——expert 层走 EP + All-to-All，非 expert 层走 TP/PP/DP。这说明同一套决策流程能覆盖 dense 和 MoE 两种模型，区别只在第 4 步是否叠加 EP。

> **面试一句话总结**：混合切分的决策流程是"先显存检查（单卡装不装得下）→ 节点内 TP → 跨节点 PP → 剩余卡做 DP/FSDP → MoE 加 EP、长序列加 SP → 最后验证通信/计算比"；记忆口诀是"TP 管放不下、PP 管节点内放不下、DP/FSDP 管快、EP 管专家、SP 管序列"——每个 P 各司其职，组合成多维并行。

---

## 附：各并行维度速查表

| 并行 | 切分维度 | 解决什么问题 | 核心通信 | 通信频率 | 适用层次 |
|---|---|---|---|---|---|
| DP | 数据（batch） | 单卡装得下但太慢 | 梯度 AllReduce | 每步 1 次 | 跨节点 |
| FSDP/ZeRO | 参数+梯度+优化器状态 | DP 显存浪费 | AllGather + ReduceScatter | 每层多次 | 跨节点 |
| TP | 层内权重矩阵 | 单卡放不下模型 | AllReduce / AllGather | 每层 2 次 | 节点内（NVLink） |
| PP | 层（stage） | 节点内放不下 | 段间激活/梯度 | 每 micro-batch 1 次 | 跨节点 |
| SP | 序列（激活/KV） | 激活显存爆炸、序列太长 | AllGather/ReduceScatter、Ring | 每层 1~2 次 | TP 组内 |
| EP | 专家 | MoE expert 多且负载不均 | All-to-All | 每层 2 次 | 节点内/跨节点 |

> **全篇总结**：分布式并行的本质是"按维度切分 + 按需通信"——每个 P 切一个维度（数据/权重/层/序列/专家），用一组通信原语把切开的计算拼回正确结果；混合并行（3D/4D）就是按"通信频率决定物理距离"的原则把各维度叠加，让每个 P 在它最擅长的层次各司其职，从而同时满足模型规模、卡间拓扑、显存与吞吐四个约束。相关通信原语（NCCL / Ring AllReduce / Tree AllReduce / NVLink / RDMA 等）详见 `Communication.md`。


